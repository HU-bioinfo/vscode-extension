import * as assert from 'assert';
import { TEST_MODE } from '../setup';

// VS Code APIの単純なテスト

describe('VS Code API テスト', () => {
  it('テスト環境でVS Code APIが正しくモック化されている', () => {
    assert.ok(true, 'モック環境で実行されています');
  });
}); 