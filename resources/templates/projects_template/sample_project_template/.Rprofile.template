source("renv/activate.R")

# プロジェクト固有の設定
options(
  repos = c(CRAN = "https://cloud.r-project.org/"),
  width = 120,
  scipen = 999,  # 科学的表記の抑制
  warnPartialMatchArgs = TRUE,
  warnPartialMatchDollar = TRUE,
  warnPartialMatchAttr = TRUE
)

# プロジェクトライブラリへのパスを設定
.libPaths(c("renv/library/R-4.3/x86_64-pc-linux-gnu", .libPaths()))

# プロジェクト開始時のメッセージ
cat("プロジェクト環境を読み込みました。\n")
cat("renvを使用してパッケージを管理しています。\n")
cat("renv::status()で現在の状態を確認できます。\n")
cat("renv::restore()で依存関係を復元できます。\n") 